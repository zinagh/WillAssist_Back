# DataBase Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/key-chat?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# server name
spring.application.name=user-management
server.servlet.context-path=/user
# server configuration
server.port=0
MIN_PORT=8081
Max_PORT=65000



#keycloak Configuration
realm=willAssist
client-id=willAssist
server-url=http://localhost:8080
grant-type=password
name=zinagh
password=Loklok20


#jwt jwk issuer
spring.security.oauth2.resourceserver.jwt.issuer-uri= http://localhost:8080/realms/willAssist
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

## JWT conversion
principle-attribute=preferred_username
resourceId=realm_access


#eureka configuration
eureka.instance.hostname=localhost
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka
eureka.client.registry-fetch-interval-seconds=10
eureka.instance.lease-renewal-interval-in-seconds=10
eureka.instance.lease-expiration-duration-in-seconds=10